import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
    goalSub: {
        marginLeft: "25px",
        marginRight: "25px",
        marginTop: "16px"
    },

    goalMessage: {
        fontSize: "12px",
        fontWeight: "600"
    }
})

export default styles;